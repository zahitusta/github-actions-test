name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello, World!"
      - name: multiple script
        run: |
          node -v
          npm -v
      - name: echoff a string
        run: echfo "Hello, World!"