name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello, World!"
      - name: multiple script
        run: |
          node -v
          npm -v
 
      - name: make a dir
        run: mkdir test
      - name: make a dir
        run: mkdir /Users/mymac/github-actions/test4
      - name: command
        run: ls 